# Domain documentation

This repository uses a single domain context.

Before exploring or changing domain behavior, read:

- `docs/CONTEXT.md`
- relevant decisions under `docs/adr/`

Use the canonical terms defined in `docs/CONTEXT.md`. Do not introduce synonyms
that its glossary explicitly rejects.

If work contradicts an existing ADR, identify the conflict rather than silently
overriding the decision.
