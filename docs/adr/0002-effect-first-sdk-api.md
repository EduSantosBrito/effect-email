# Effect-First SDK API

Effect Email v0 exposes an Effect `Context.Service`, Effect Layers, and small helper functions instead of a `createClient()` factory. A single Effect-first API keeps dependency injection, testing, tracing, typed failures, and secret handling in one model instead of securing and documenting parallel SDK shapes.
